spring.cloud.nacos.config.file-extension=properties
spring.application.name=message-cluster
logging.file.path=/data/logs/${spring.application.name}
logging.file.name=/data/logs/${spring.application.name}/${spring.application.name}.log
